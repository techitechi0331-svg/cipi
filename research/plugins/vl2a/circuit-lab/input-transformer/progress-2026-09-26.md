# VL2A Circuit Lab — Priority 1 progress snapshot

Date: 2026-09-26

## Current location

Priority 1 — UTC HA-100X input transformer.

## Completed

- current RC2 input TransformerModel audited;
- current block classified HEURISTIC;
- manufacturer/catalog identity and electrical envelope collected;
- UTC terminal-arrangement topology collected;
- LA-2A system response constraint collected;
- lower-tier DCR / inductance measurements separated from SOURCE_FACT;
- offline ideal/load-aware model implemented;
- offline LTI parameterized model implemented;
- current heuristic numerically probed;
- transformer-specific benchmark contract created;
- compiled research branch/harness created without touching integration/vl2a-v060-rc2;
- whole-engine PR0 30/1k/15k measurement added to compiled harness.

## Important new finding

Current low-level 12 Hz high-pass contributes about -0.64 dB at 30 Hz relative to 1 kHz.

The official LA-2A whole-system claim is +/-0.1 dB from 30 Hz to 15 kHz.

Phase01H does not provide compensating bass boost.

Classification:
INFERRED fidelity mismatch pending compiled whole-engine confirmation.

## New physical clue

Lower-tier reported HA-100X secondary inductance near 2128–2154 H at 120 Hz, if referred through the nominal ~10 ratio, suggests a primary-referred Lm around 21.3 H.

A simple linear sensitivity model with that prior predicts far less 30 Hz loss than the current 12 Hz high-pass, even with a 600-ohm source.

Classification:
lower-tier MEASURED -> INFERRED prior, not product constant.

## REJECTED

- equating input impedance 600 ohms with universal source resistance 600 ohms;
- treating 12 Hz HPF as a measured HA-100X pole;
- treating 25 ms state as proven magnetic memory;
- unlocking hysteresis before a linear reference leaves a hardware-supported residual;
- sending generic MELON compressor fitness directly at the transformer.

## BLOCKED_EXTERNAL

Compiled self-hosted research workflow result is not currently enumerable through the available connector path.

This blocks only the compiled-result ingestion node.

## Work stolen while blocked

- UTC terminal topology research;
- source/load uncertainty refinement;
- lower-tier inductance analysis;
- whole-system frequency-response falsification design;
- transformer-specific MELON benchmark contract;
- expanded compiled harness.

## Next

1. ingest compiled baseline-vs-linear isolation when artifact becomes available;
2. compare PR0 full-engine 30/1k/15k response;
3. verify PR50 COMP/LIMIT GR delta after removal of transformer tanh;
4. reconstruct exact LA-2A secondary loading as a network rather than a single nominal resistor;
5. then expose Candidate C parameter space to MELON under the new benchmark contract.
